name: Sync Code to Friend Repo

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Sync to friend's repo
        env:
          FRIEND_TOKEN: ${{ secrets.LIUHAO }}
        run: |
          # 配置朋友的身份
          git config user.name "HaoLiu923"
          git config user.email "405685255@qq.com"
          
          # 删除 .github 目录（不同步工作流）
          rm -rf .github
          
          # 添加朋友的 repo 作为 remote
          git remote add friend https://HaoLiu923:${FRIEND_TOKEN}@github.com/HaoLiu923/DynHyperRAG.git
          
          # 强制推送到朋友的 repo（覆盖式同步）
          git push -f friend main:main
